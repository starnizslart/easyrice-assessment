version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: easyrice-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: easyrice_password
      MYSQL_DATABASE: easyrice_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: